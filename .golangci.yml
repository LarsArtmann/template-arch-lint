# 🔥 ENTERPRISE-GRADE GOLANGCI-LINT CONFIGURATION
# 🚨 MAXIMUM STRICTNESS - ZERO TOLERANCE FOR CODE QUALITY VIOLATIONS
#
# This configuration enforces the highest standards of Go code quality
# Designed for mission-critical production systems with zero defect tolerance

# version: 1 (default, compatible with golangci-lint v1.64.8)

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
  modules-download-mode: readonly
  allow-parallel-runners: true
  go: "1.23"

# 🔥 LINTERS - Maximum enforcement (essential set)
linters:
  enable:
    # 🚨 TYPE SAFETY ENFORCEMENT
    - forbidigo      # Custom forbidden patterns
    - govet          # Suspicious constructs
    - staticcheck    # Advanced static analysis

    # 🛡️ ERROR HANDLING
    - errcheck       # Unchecked errors
    - errorlint      # Error wrapping
    - nilerr         # Nil error returns

    # 🔒 SECURITY
    - gosec          # Security audit
    - copyloopvar    # Loop variable copies
    - noctx          # HTTP context validation
    - wrapcheck      # Error wrapping enforcement

    # 📊 CODE QUALITY
    - bodyclose      # HTTP body closure
    - cyclop         # Cyclomatic complexity
    - exhaustive     # Switch statement completeness
    - funlen         # Function length
    - gocognit       # Cognitive complexity
    - gocyclo        # Cyclomatic complexity
    - goconst        # Repeated constants
    - gocritic       # Opinionated checks
    - gomoddirectives # go.mod validation
    - goprintffuncname # Printf function naming
    - ineffassign    # Ineffective assignments
    - misspell       # Spelling errors
    - nakedret       # Naked returns
    - nestif         # Deep nesting prevention
    - nolintlint     # Nolint comment validation
    - prealloc       # Slice preallocation
    - revive         # Comprehensive checks
    - rowserrcheck   # SQL rows error handling
    - sqlclosecheck  # SQL resource closure
    - tparallel      # Test parallelism
    - unconvert      # Unnecessary conversions
    - unparam        # Unused parameters
    - unused         # Unused code
    - whitespace     # Trailing whitespace
    - godot          # Comment punctuation

    # 🏗️ ARCHITECTURE & BEST PRACTICES
    - gochecknoinits # No init functions (anti-pattern)
    - gochecknoglobals # No global variables
    - lll            # Line length limits
    - maligned       # Struct alignment optimization
    - golint         # Go style guide enforcement
    - gomnd          # Magic number detection

    # 💀 DEAD CODE DETECTION
    - deadcode       # Unreachable code
    - varcheck       # Unused global variables
    - structcheck    # Unused struct fields


# 🔧 LINTER SETTINGS - Maximum strictness
linters-settings:
  # 🚨 TYPE SAFETY & STRUCTURED LOGGING - ZERO TOLERANCE
  forbidigo:
    forbid:
      # Type safety violations
      - p: 'interface\{\}'
        msg: "🚨 BANNED: interface{} erases type safety. Use specific types or generics"
      - p: '\bany\b'
        msg: "🚨 BANNED: any erases type safety. Use specific types or generics"
      - p: 'panic\('
        msg: "🚨 BANNED: panic() crashes programs. Return errors instead"
      
      # Structured logging enforcement (inspired by Uniflow)
      - p: '^fmt\.Print.*$'
        msg: "🚨 BANNED: Do not commit print statements. Use github.com/charmbracelet/log for structured logging"
      - p: '^log\..*$'
        msg: "🚨 BANNED: Do not use standard log package. Use github.com/charmbracelet/log for structured logging"  
      - p: '^print$|^println$'
        msg: "🚨 BANNED: Do not commit print statements. Use structured logging instead"
      - p: 'fmt\.Printf.*'
        msg: "🚨 BANNED: Use structured logging instead of fmt.Printf. Prefer log.Info() with fields"

    analyze-types: true

  # Cyclomatic complexity
  cyclop:
    max-complexity: 10
    package-average: 5.0
    skip-tests: false

  gocyclo:
    min-complexity: 10

  # Cognitive complexity
  gocognit:
    min-complexity: 10

  # Function length
  funlen:
    lines: 50
    statements: 30
    ignore-comments: false

  # Error handling
  errcheck:
    check-type-assertions: true
    check-blank: true
    # 🆕 Function-specific exclusions (smart defaults)
    exclude-functions:
      # Resource cleanup - errors in defer contexts are typically non-critical
      - (io.Closer).Close
      - (*os.File).Close
      - (net.Conn).Close
      - (*net.TCPConn).Close
      - (*net.UDPConn).Close
      - (*sql.DB).Close
      - (*sql.Rows).Close
      - (*sql.Stmt).Close
      
      # Formatting operations - rarely fail and errors are usually non-critical
      - fmt.Fprint
      - fmt.Fprintf  
      - fmt.Fprintln
      - fmt.Print
      - fmt.Printf
      - fmt.Println
      
      # Buffer operations - bytes.Buffer operations never fail
      - (*bytes.Buffer).Write
      - (*bytes.Buffer).WriteByte
      - (*bytes.Buffer).WriteRune
      - (*bytes.Buffer).WriteString
      
      # String builder operations - strings.Builder operations never fail
      - (*strings.Builder).Write
      - (*strings.Builder).WriteByte
      - (*strings.Builder).WriteRune  
      - (*strings.Builder).WriteString

  errorlint:
    errorf: true
    errorf-multi: true
    asserts: true
    comparison: true

  # Security
  gosec:
    severity: low
    confidence: low
    excludes: []

  # Static analysis
  staticcheck:
    checks: ["all"]

  stylecheck:
    checks: ["all"]

  # Go critic
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks: []

  # Unused code
  unused: {}

  unparam:
    check-exported: true

  # Revive rules
  revive:
    severity: error
    enable-all-rules: true
    rules:
      # 📏 FILE LENGTH ENFORCEMENT
      - name: file-length-limit
        severity: error
        arguments: [400]

  # Nesting complexity
  nestif:
    min-complexity: 4

  # Exhaustiveness
  exhaustive:
    check-generated: false
    default-signifies-exhaustive: true

  # Preallocation
  prealloc:
    simple: true
    range-loops: true
    for-loops: false

  # Error wrapping
  wrapcheck:
    ignoreSigs:
      - .Errorf(
      - errors.New(
      - errors.Unwrap(
      - .Wrap(
      - .Wrapf(
    ignoreSigRegexps:
      - \.New.*Error\(
    ignorePackageGlobs:
      - encoding/*
      - github.com/pkg/*

  # Comment punctuation
  godot:
    scope: declarations
    exclude: []
    capital: false

  # 🏗️ ARCHITECTURE ENFORCEMENT
  gochecknoinits: {}
  
  gochecknoglobals:
    # Allow global variables only in main packages and tests
    exclude-files:
      - "main\\.go$"
      - ".*_test\\.go$"

  # Line length limits
  lll:
    line-length: 120
    tab-width: 4

  # Struct alignment optimization
  maligned:
    suggest-new: true

  # Magic number detection
  gomnd:
    # Ignored numbers (common constants that don't need extraction)
    ignored-numbers:
      - '0'
      - '1' 
      - '2'
      - '10'
      - '100'
      - '1000'
    # Ignored functions where numbers are acceptable
    ignored-functions:
      - 'strconv.ParseInt'
      - 'strconv.ParseFloat'
      - 'time.Sleep'
      - 'time.Duration'

# 🚨 ISSUES CONFIGURATION
# Note: Formatters (gofumpt, goimports) should be run separately via 'just fix'
issues:
  max-issues-per-linter: 0
  max-same-issues: 0

  exclude-use-default: false
  exclude-rules-use-default: false

  # Common false positives preset
  exclude-dir-use-default: false

  exclude-rules:
    # Allow panics only in main/cmd
    - path: ^(main\.go|cmd/.+)$
      linters:
        - forbidigo
      text: "panic"

    # Allow longer functions in tests
    - path: _test\.go
      linters:
        - funlen
        - gocognit
        - gocyclo
        - cyclop

    # 📏 File length exemptions for generated files
    - path: ".*_gen\\.go$|.*\\.pb\\.go$|.*_templ\\.go$|.*\\.sql\\.go$|internal/db/.*\\.go$"
      linters:
        - revive
      text: "file-length-limit"

    # Allow longer test files for comprehensive test suites
    - path: "_test\\.go$"
      linters:
        - revive
      text: "file-length-limit"

  exclude-dirs:
    - vendor
    - node_modules   # Frontend dependencies 
    - testdata
    - examples
    - docs
    - generated
    - mocks
    - .git
    - .github

  exclude-files:
    - ".*\\.pb\\.go$"
    - ".*_generated\\.go$"
    - ".*_gen\\.go$"
    - "mock_.*\\.go$"

# 📊 OUTPUT CONFIGURATION
output:
  print-issued-lines: true
  print-linter-name: true
  sort-results: true
